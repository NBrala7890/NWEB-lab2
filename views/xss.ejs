<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Security</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>XSS Demonstration</h1>
    <p>Current XSS Status: <strong><%= xssEnabled ? "Vulnerable" : "Secure" %></strong></p>

    <form action="/submit" method="POST">
        <label for="userInput">Enter text:</label>
        <input type="text" id="userInput" name="userInput">
        <button type="submit">Submit</button>
    </form>

    <% if (typeof displayText !== 'undefined') { %>
        <h2>Output:</h2>
        <div class="output-text"><%- displayText %></div>
    <% } %>

    <form action="/toggle-xss" method="POST" class="btn">
        <button type="submit">Toggle XSS</button>
    </form>

    <button onclick="window.location.href='/'">Return to Homepage</button>
</body>
</html>
